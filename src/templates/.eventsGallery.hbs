{{#each this}}
<li class="events__item">
    <div class="music__thumb">
        <picture class="music__img">
            <source srcset="{{}}" />
            <source srcset="{{}}" />
            <img class="music__img-poster img" src="{{}}" data-source="{{}}" alt="{{}}">
        </picture>
    </div>
    <div class="music__info">
        <h3 class="events__title title">{{}}</h3>
        <p class="events__text text">{{}}</p>
        <a href="#" class="music__link">
            <span class="material-icons">room</span>
            {{}}
        </a>
    </div>
</li>
{{/each}}